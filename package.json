{
  "name": "urlz",
  "description": "Functional utils for URLs",
  "version": "0.3.1",
  "author": "Ivan Kleshnin <ivan@paqmind.com> (paqmind.com)",
  "license": "MIT",
  "bugs": "https://github.com/ivan-kleshnin/urlz/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/ivan-kleshnin/urlz"
  },
  "keywords": [
    "url",
    "path",
    "query",
    "functional",
    "fp"
  ],
  "main": "index.js",
  "dependencies": {
    "pathz": "^0.9.0"
  },
  "peerDependencies": {
    "@paqmind/ramda": "*"
  },
  "devDependencies": {
    "@paqmind/ramda": "*",
    "mocha": "^3.2.0"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "prebuild": "rm -f *.js",
    "build": "babel src/ -d ./",
    "build-watch": "babel --watch src/ -d ./",
    "prepare": "npm run build",
    "test": "mocha tests/ --recursive"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
